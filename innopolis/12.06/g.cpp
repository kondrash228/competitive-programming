#include <iostream>
#include <vector>
#include <algorithm>

// проверить есть ли циклы в графе

using namespace std;
typedef long long ll;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

}